<div>
  <%= link_to 'Admin Login', list_subjects_path %>
</div>

<ul id="subjects">
<% Subject.find(:all).each do |c| %>
<li><%= link_to c.name, :action => "show_subjects",
:id => c.id %></li>
<% end %>
</ul>

<!--# this part is not required for user-->

<!--<p align="left"><%= link_to "Add new Book", new_book_path %></p>

<% if @books.blank? %>
<p>There are not any books currently in the system.</p>
<% else %>
<p>These are the current books in our system</p>
<ul id="books">
<% @books.each do |c| %>
  <br></br>
<li><%= link_to c.title, {:action => 'show', :id => c.id} -%>
  <br></br>
<b> <%= link_to 'Edit', {:action => "edit",:id => c.id} %></b>

<b> <%= link_to "Delete", {:action => 'delete', :id => c.id},:confirm => "Are you sure you want to delete this item?" %></b></li>




<% end %>
</ul>
<% end %>-->

